import { CheckAlphabetsPipe } from './check-alphabets.pipe';

describe('CheckAlphabetsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckAlphabetsPipe();
    expect(pipe).toBeTruthy();
  });
});
